post_install() {
    cat <<'EOF'

Claudezilla is installed. Finish setup for your user with:

    claudezilla-setup

That registers the MCP server with Claude Code and sideloads the extension
into a dedicated Firefox profile.
EOF
}

post_upgrade() {
    post_install
}
